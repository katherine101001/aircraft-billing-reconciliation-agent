# Dev/test dependencies only (not needed at runtime). To measure coverage:
#   pip install -r requirements-dev.txt
#   python -m coverage run -m tests.run_all
#   python -m coverage report -m
coverage>=7.0
