# Copy this file to .env and fill in a real key.
# Leave it blank if you have no LLM API key — the program falls back to the
# mock text layer, and the reconciliation engine still runs. The real-LLM
# insertion point is marked in src/ai_layer.py.
OPENAI_API_KEY=
